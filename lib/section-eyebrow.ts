/**
 * The small label above a section title — "What I do", "Selected work",
 * "Numbers, not claims".
 *
 * Shared rather than repeated because it already drifted once: Services and the
 * proof block settled on uppercase at `font-semibold` with wide tracking, while
 * Projects kept an older sentence-case `font-medium` treatment, and the two sat a
 * few hundred pixels apart on the same page.
 */
export const SECTION_EYEBROW =
  "text-xs font-semibold uppercase tracking-[0.14em] text-zinc-500 dark:text-white/45 sm:text-sm";
